USE MyDatabase

GO

-- INSERT DATA FROM CUSTOMERS TABLE INTO PERSON

INSERT INTO PERSON(ID, PERSON_NAME, BIRTH_DATE, PHONE)
SELECT 
ID,
first_name,
NULL,
'UNKNOWN'
FROM customers

SELECT *FROM PERSON

